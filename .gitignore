# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# Virtualenvs
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Checkpoints / data / engines / run outputs (never commit large artifacts).
# Anchored to repo root so they do not shadow source packages like
# src/gps/data/ (the ingestion code, which MUST be committed).
/checkpoints/
/data/
/runs/
/wandb/
*.pgn
*.sgf
*.bin
*.nnue
engines/

# Editor / OS
.DS_Store
.idea/
.vscode/
